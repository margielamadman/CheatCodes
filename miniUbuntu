//Following this guide: https://ubuntuforums.org/showthread.php?t=2361552

//Download the mini.iso file from: http://archive.ubuntu.com/ubuntu/dists/bionic-updates/main/installer-amd64/current/images/netboot/

//Mount the mini.iso and copy the file to a folder
$ sudo mkdir /media/iso
$ sudo mount -o loop ~/Downloads/mini.iso /media/iso
$ cp -r /media/iso/ ~/Documents/mini

//Change folder permissions and locate the efi.img
$ chmod -R 744 mini/
$ cd ~/Documents/mini/boot/grub/

//Mount the efi.img
$ sudo mkdir /media/tmp
$ sudo mount -t auto -o loop efi.img /media/tmp

//Copy contents of efi.img to mini dir and unmount drives
$ cp -r /media/tmp/efi/ ~/Documents/mini
$ sudo umount /media/iso
$ sudo umount /media/tmp

//Format USB Drive
$ sudo fdisk -l //To find drive
$ sudo wipefs --all /dev/<drive> --force //Wipes existing filesystem
$ sudo cfdisk /dev/<drive> //Create dos partition
	//Select dos and press enter
	//Keep drive at max size
	//Choose primary partition
	//Choose [Bootable] to make the drive bootable
	//Select [Write] and and type yes to make the changes
	//Select [Quit] to exit
$ sudo fdisk -l //Check for new drive
$ sudo mkfs.vfat -n 'MINIUBUNTU' /dev/sdb1

//Copy contents of mini dir to formatted USB drive
	//Unplug and replug drive in, it should automount
$ lsblk //To see the mount point of the USB drive
$ cp -r ~/Documents/mini/* <mountpoint of drive>

//Reboot and boot from this USB drive
//After install and on your first boot
	//press "e" during the grub install
	//on the linux line remove "$vt_handoff"
	//press f10 to boot into your new build
